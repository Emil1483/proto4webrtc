# Copy to .env next to compose.yml and fill in.
# Image tag to deploy (e.g. a git sha or "latest").
TAG=latest

# Auth (see server/.env.example). The server rejects every signaling
# connection until AUTH_PASSWORD and ROBOT_TOKEN are set.
AUTH_PASSWORD=
AUTH_SECRET=
ROBOT_TOKEN=
