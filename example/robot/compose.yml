services:
  ros2:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    network_mode: host
    # ROBOT_TOKEN (robot auth) comes from the single example/.env.
    env_file:
      - ../.env
